# Resync - HCL Workload Automation AI Interface
# Requirements File

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
starlette>=0.35.0
python-multipart>=0.0.6

# Database - PostgreSQL (Production)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0         # PostgreSQL async driver (REQUIRED for production)
psycopg2-binary>=2.9.0  # PostgreSQL sync driver (for migrations)
alembic>=1.13.0         # Database migrations

# Database - SQLite (Development only)
aiosqlite>=0.19.0       # SQLite async driver (development)

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# HTTP Client
httpx>=0.26.0
aiohttp>=3.9.0

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0
toml>=0.10.0

# File watching (for hot-reload)
watchdog>=3.0.0

# System monitoring
psutil>=5.9.0

# LLM Integration
litellm>=1.0.0
openai>=1.0.0
anthropic>=0.18.0

# Vector Store / RAG
qdrant-client>=1.7.0

# Document Processing
beautifulsoup4>=4.12.0   # HTML parsing for IBM/HCL documentation
lxml>=5.0.0              # Fast HTML/XML parser backend for BeautifulSoup
pypdf>=3.17.0            # PDF text extraction (already in use)

# Caching
redis>=5.0.0
aiocache>=0.12.0

# Logging
structlog>=24.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Optional: OpenTelemetry (observability)
# opentelemetry-api>=1.22.0
# opentelemetry-sdk>=1.22.0
# opentelemetry-instrumentation-fastapi>=0.43b0
# opentelemetry-instrumentation-httpx>=0.43b0
